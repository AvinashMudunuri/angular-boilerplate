import { SortRatingsPipe } from './sort-ratings.pipe';

describe('SortRatingsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortRatingsPipe();
    expect(pipe).toBeTruthy();
  });
});
